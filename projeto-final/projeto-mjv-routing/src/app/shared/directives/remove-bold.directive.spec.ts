import { RemoveBoldDirective } from './remove-bold.directive';

describe('RemoveBoldDirective', () => {
  it('should create an instance', () => {
    const directive = new RemoveBoldDirective();
    expect(directive).toBeTruthy();
  });
});
